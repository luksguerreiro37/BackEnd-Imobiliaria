import {
  userSchema,
  userCreateSchema,
  userUpdateSchema,
  userReadSchema,
  userReturnSchema,
} from "./user.schemas";

import { categoryCreateSchema, categoryReadSchema } from "./category.schemas";

import { addressSchema, addressCreateSchema } from "./address.schemas";

import { realEstateSchema, realEstateReadSchema } from "./realEstate.schemas";

import {
  scheduleSchema,
  scheduleCreateSchema,
  scheduleReadSchema,
} from "./schedule.schemas";

import { sessionSchema } from "./session.schemas";

export {
  userSchema,
  userCreateSchema,
  userUpdateSchema,
  userReadSchema,
  userReturnSchema,
  categoryCreateSchema,
  categoryReadSchema,
  addressSchema,
  realEstateSchema,
  realEstateReadSchema,
  addressCreateSchema,
  scheduleSchema,
  scheduleCreateSchema,
  scheduleReadSchema,
  sessionSchema,
};
